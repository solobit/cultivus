

@charset 'UTF-8'

@import 'core'

/*
 core
    \- appearance
    \- controls
    \- staging -
                I@ preamble
                           - typesetting
                           - ../../themes
                           - ../../i18n
                I@ ../appearance
                I@ ../controls
                
    
    \- typesetting
//
// Function calls should all be done
// after core has been imported (chain of files)
//
*/

// apply-reset() // full reset less desirable
apply-normalize()


apply-theme(body, my-children)
    .default-stuff
        color hilight_color

apply-theme(body, 1)
/*

my-children(parent, children)
    for child, index in children
        {parent}:nth-child({index})
            height: 10px * index

my-children('table tr', (1 2 3 4))


*/